export type ArticleCardPropsType = {
    disabled?: boolean;
    date: String,
    title: String,
    comments: Number,
    views: Number,
    shares: Number,
    cover: any,
    avatar: any
    isFullCover?: boolean,
    isLargeImage?: boolean,
  };